#include <stdio.h>
/* #define vector<int> vi
#define loop(x,n) for(int x = 0; x < n; ++x) */
int factorial(int n) {
    int result = 1;
    float resul2 = 20.2;
    char a = 'a';
    for (int i = 1; i <= n; i++) {
        result = result * i;
    }
    return result;
}

void main() {
return;
}
